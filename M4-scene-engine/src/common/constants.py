"""M4 场景引擎 - 全局常量定义

集中管理项目中的魔法常量，避免硬编码散落在各处。
新增常量时请在此文件中统一定义，并保持命名规范（全大写下划线分隔）。
"""

from __future__ import annotations


# ---------------------------------------------------------------------------
# 用户相关
# ---------------------------------------------------------------------------

# 默认用户 ID，用于单用户模式或未指定用户的场景
DEFAULT_USER_ID: str = "default"


# ---------------------------------------------------------------------------
# 场景相关
# ---------------------------------------------------------------------------

# 默认场景标识（从 config.py 和 main.py 中确认）
DEFAULT_SCENE: str = "emotional"


# ---------------------------------------------------------------------------
# 主题相关
# ---------------------------------------------------------------------------

# 默认主题
DEFAULT_THEME: str = "default"


# ---------------------------------------------------------------------------
# 数据库相关
# ---------------------------------------------------------------------------

# 数据库字段默认长度
DB_USER_ID_LENGTH: int = 128
DB_SCENE_ID_LENGTH: int = 64
DB_THEME_LENGTH: int = 50


# ---------------------------------------------------------------------------
# HTTP 相关
# ---------------------------------------------------------------------------

# 默认请求超时时间（秒）
DEFAULT_REQUEST_TIMEOUT: int = 30

# 默认分页大小
DEFAULT_PAGE_SIZE: int = 20
MAX_PAGE_SIZE: int = 100


# ---------------------------------------------------------------------------
# 上下文相关
# ---------------------------------------------------------------------------

# 上下文变量默认值（空字符串）
CONTEXT_VAR_DEFAULT: str = ""


__all__ = [
    "DEFAULT_USER_ID",
    "DEFAULT_SCENE",
    "DEFAULT_THEME",
    "DB_USER_ID_LENGTH",
    "DB_SCENE_ID_LENGTH",
    "DB_THEME_LENGTH",
    "DEFAULT_REQUEST_TIMEOUT",
    "DEFAULT_PAGE_SIZE",
    "MAX_PAGE_SIZE",
    "CONTEXT_VAR_DEFAULT",
]
