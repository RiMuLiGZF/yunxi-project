# ============================================================
# 开发环境依赖
# 安装方式: pip install -r requirements-dev.txt
# ============================================================

# --- Lint & 格式化 ---
ruff>=0.6.0,<1.0.0          # 快速 Python linter + formatter（替代 flake8/isort/pyupgrade/bandit）
mypy>=1.10.0,<2.0.0         # 类型检查器（渐进式严格模式）
pre-commit>=3.7.0,<4.0.0    # Git 提交前钩子框架
codespell>=2.3.0,<3.0.0     # 代码拼写检查

# --- 测试框架 ---
pytest>=8.0.0,<9.0.0                 # 测试框架
pytest-asyncio>=0.23.0,<1.0.0        # 异步测试支持
pytest-cov>=5.0.0,<6.0.0             # 代码覆盖率统计
pytest-xdist>=3.5.0,<4.0.0           # 并行测试执行（-n auto）
pytest-timeout>=2.3.0,<3.0.0         # 测试超时控制（防止死循环）
pytest-rerunfailures>=14.0,<15.0     # 失败重跑（仅对 flaky 标记的测试）
pytest-html>=4.0.0,<5.0.0            # HTML 测试报告
pytest-mock>=3.14.0,<4.0.0           # Mock 工具增强

# --- 代码质量 & 安全 ---
bandit>=1.7.0,<2.0.0        # Python 安全扫描（检测常见安全漏洞）
radon>=6.0.0,<7.0.0         # 代码复杂度和可维护性分析

# --- HTTP 测试 ---
httpx>=0.27.0,<1.0.0        # 异步 HTTP 客户端（FastAPI 测试）
fastapi>=0.110.0,<1.0.0     # Web 框架（测试依赖）
uvicorn>=0.27.0,<1.0.0      # ASGI 服务器

# --- 其他 ---
# 注意：项目主要依赖请参见各模块的 requirements.txt
