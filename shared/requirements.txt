# ============================================================
# shared - 云汐公共组件 依赖声明
# 各模块使用 shared 时需安装这些依赖
# ============================================================

# ---- Web 框架 ----
# SEC-013: 关键安全依赖使用 ~= 格式，只允许补丁版本升级
fastapi~=0.115.0
uvicorn[standard]~=0.30.0
pydantic~=2.9.0

# ---- 配置管理 ----
python-dotenv~=1.0.0
pyyaml~=6.0.1

# ---- 日志 ----
structlog>=23.0.0

# ---- 系统监控 ----
psutil>=5.9.0

# ---- 加密 ----
# SEC-013: 加密库版本必须严格控制
cryptography~=43.0.0

# ---- 数据库 ----
sqlalchemy~=2.0.35

# ---- 缓存 / 消息队列 ----
redis>=5.0.0

# ---- 工具 ----
httpx~=0.27.0
aiofiles>=23.0.0

# ---- 语音VAD ----
silero-vad>=4.0
numpy>=1.24.0
# webrtcvad 可选（Windows编译问题，默认使用silero-vad）
